# required vars:
# - uid
# - width
# - height

# obj:  
  id: ${uid}_container
  width: ${width}
  height: ${height}
  widgets:
    - label:
        id: ${uid}_widget_unknown
        text: ${label}
        align: TOP_LEFT
    - label:
        id: ${uid}_label
        text: "--"
        align: center
